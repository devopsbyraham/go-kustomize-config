apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: go-kustomize-deployment:/Service:default/go-api-svc
  name: go-api-svc
  namespace: default
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: go-api
